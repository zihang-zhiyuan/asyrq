# asyrq 使用说明文档

> Python 1:1 复刻 Go 语言 [asynq](https://github.com/hibiken/asynq) 的 Redis 分布式任务队列
> 核心通信: redis-py 8.x 原生异步 I/O

---

## 目录

1. [安装](#1-安装)
2. [5 分钟快速上手](#2-5-分钟快速上手)
3. [Task — 任务](#3-task--任务)
4. [Options — 全部选项](#4-options--全部选项)
5. [Client — 生产者](#5-client--生产者)
6. [Server — 消费者](#6-server--消费者)
7. [FastAPI 风格装饰器](#7-fastapi-风格装饰器)
8. [按任务类型控制并发](#8-按任务类型控制并发)
9. [ResultKey — 结果存储](#9-resultkey--结果存储)
10. [Scheduler — 定时调度](#10-scheduler--定时调度)
11. [Config 完整配置](#11-config-完整配置)
12. [中间件](#12-中间件)
13. [错误处理 & 重试](#13-错误处理--重试)
14. [监控 & 运维](#14-监控--运维)
15. [爬虫场景最佳实践](#15-爬虫场景最佳实践)

---

## 1. 安装

```bash
pip install asyrq
```

**依赖：**

| 组件 | 版本 | 说明 |
|------|------|------|
| Python | ≥ 3.10 | 核心语言 |
| Redis 服务器 | ≥ 5.0 | Lua 脚本支持 |
| redis-py | ≥ 8.0.0 | Python ↔ Redis 通信 |
| croniter | ≥ 2.0.0 | Cron 定时解析 |

---

## 2. 5 分钟快速上手

**终端 1 — 启动消费者：**

```python
# consumer.py
import asyncio, json
from asyrq import Server, Config, RedisClientOpt, Context, Task

app = Server(
    RedisClientOpt(addr="127.0.0.1:6379", password=""),
    Config(name="我的消费者", concurrency=10),
)

@app.task("email:send")
async def handle_email(ctx: Context, task: Task):
    data = json.loads(task.payload())
    print(f"发送邮件给: {data['to']}")
    # 结果自动存到 email:send:result:{task_id}
    writer = task.result_writer()
    await writer.write(json.dumps({"status": "ok"}).encode())

asyncio.run(app.run())  # Ctrl+C 停止
```

**终端 2 — 生产任务：**

```python
import asyncio, json
from asyrq import Client, Task, RedisClientOpt

async def main():
    client = Client(RedisClientOpt(addr="127.0.0.1:6379"))
    await client.enqueue(Task("email:send", json.dumps({"to": "hi@x.com"}).encode()))
    await client.close()

asyncio.run(main())
```

---

## 3. Task — 任务

```python
from asyrq import Task

# 基本任务
task = Task("email:send", json.dumps({"to": "a@b.com"}).encode())

# 带请求头
task = Task("report:gen", payload, headers={"trace-id": "abc"})

# task_id 自动生成（UUID），也可手动指定
task = Task("sms:send", payload)

# 读属性
task.type()     # "sms:send"
task.payload()  # b'...'
task.headers()  # {"trace-id": "abc"}
```

## 4. Options — 全部选项

| 选项 | 说明 | 示例 |
|------|------|------|
| `Queue("name")` | 指定队列 | `Queue("critical")` |
| `MaxRetry(n)` | 最大重试次数（默认 25） | `MaxRetry(3)` |
| `Timeout(secs)` | 处理超时 | `Timeout(60)` |
| `ProcessIn(secs)` | 延迟执行 | `ProcessIn(30)` |
| `ProcessAt(nsec)` | 指定时间戳执行 | `ProcessAt(ts)` |
| `Unique(ttl)` | TTL 内去重 | `Unique(3600)` |
| `Retention(secs)` | 完成后保留 | `Retention(600)` |
| `Group(name)` | 聚合组 | `Group("batch")` |
| `TaskID(id)` | 自定义 ID | `TaskID("my-id")` |
| `ResultKey(key)` | 自定义结果 key | `ResultKey("my:out")` |

## 5. Client — 生产者

```python
client = Client(RedisClientOpt(addr="127.0.0.1:6379", password="", db=0))

# 即时执行
await client.enqueue(task)

# 延迟 30 秒
await client.enqueue(task, ProcessIn(30))

# 高优队列
await client.enqueue(task, Queue("critical"))

# 去重（60 秒内不重复）
await client.enqueue(task, Unique(60))

# 自定义结果 key
await client.enqueue(task, ResultKey("my:output"))

# 组合使用
await client.enqueue(task, Queue("critical"), MaxRetry(3), ResultKey("out"))

await client.close()
```

### 底层实现

```bash
# 入队时执行原子 Lua 脚本:
HSET asynq:{default}:t:{task_id} msg {...json...} state pending
LPUSH asynq:{default}:pending {task_id}

# Server 取任务:
RPOP asynq:{default}:pending  →  LPUSH asynq:{default}:active
HGET asynq:{default}:t:{task_id} msg  → 反序列化  → 调 handler

# 完成:
done.lua: LREM active + ZADD completed
```

## 6. Server — 消费者

### FastAPI 风格（推荐）

```python
app = Server(redis, Config(name="消费者", code="worker-01", concurrency=10))

@app.task("email:send")
async def handle_email(ctx, task):
    ...

asyncio.run(app.run())  # 阻塞运行
```

### 传统风格

```python
mux = ServeMux()
mux.handle_func("email:send", handle_email)
server = Server(redis, Config(concurrency=10))
await server.run(mux)
```

---

## 7. FastAPI 风格装饰器

```python
app = Server(redis, Config(name="服务", code="svc-01"))

@app.task("order:create")
async def create(ctx, task): ...

@app.task("order:pay")
async def pay(ctx, task): ...

# 中间件
app.use(logging_middleware)

# 启动
await app.run()
```

## 8. 按任务类型控制并发

```python
import asyncio

_email = asyncio.Semaphore(1)     # 串行：一次只能 1 个
_report = asyncio.Semaphore(5)    # 并发：最多 5 个

@app.task("email:send")
async def handle_email(ctx, task):
    async with _email:   # ← 关键：Semaphore 控制
        ...

@app.task("report:gen")
async def handle_report(ctx, task):
    async with _report:
        ...
```

## 9. ResultKey — 结果存储

```python
# 不传 ResultKey → 自动存到 {task_type}:result:{task_id}
await client.enqueue(Task("email:send", payload))
# 结果: email:send:result:a1b2c3d4...

# 传 ResultKey → 存到指定 key:{task_id}
await client.enqueue(Task("email:send", payload), ResultKey("my:out"))
# 结果: my:out:a1b2c3d4...
```

Handler 中正常写结果即可自动同步：

```python
@app.task("email:send")
async def handler(ctx, task):
    result = await do_work()
    writer = task.result_writer()
    await writer.write(json.dumps(result).encode())
```

## 10. Scheduler — 定时调度

```python
from asyrq import Scheduler

scheduler = Scheduler(RedisClientOpt(addr="127.0.0.1:6379"))

# Cron 表达式
await scheduler.register("0 9 * * *", task)     # 每天 9:00
await scheduler.register("*/5 * * * *", task)   # 每 5 分钟

# @every 格式
await scheduler.register("@every 30s", task)    # 每 30 秒
await scheduler.register("@every 1h", task)     # 每小时

# 取消注册
entry_id = await scheduler.register("@every 1m", task)
await scheduler.unregister(entry_id)

await scheduler.run()  # 阻塞运行
```

## 11. Config 完整配置

```python
Config(
    name="消费者名称",        # 显示名称
    code="worker-01",          # Redis server_id
    concurrency=10,            # 全局并发数
    queues={                   # 队列权重
        "critical": 6,
        "default": 3,
        "low": 1,
    },
    strict_priority=False,     # 严格优先级
    shutdown_timeout=8,        # 关闭超时秒数
    retry_delay_func=my_delay, # 自定义重试延迟
    error_handler=my_handler,  # 错误回调
    log_dir="logs",            # 日志目录
    log_level=LogLevel.INFO,   # 日志级别
)
```

## 12. 中间件

```python
def timing_middleware():
    def middleware(handler):
        class Wrapper(Handler):
            async def process_task(self, ctx, task):
                t0 = time.time()
                await handler.process_task(ctx, task)
                print(f"{task.type()} 耗时 {time.time()-t0:.2f}s")
        return Wrapper()
    return middleware

app.use(timing_middleware())
```

## 13. 错误处理 & 重试

```python
from asyrq import SkipRetry

@app.task("my:task")
async def handler(ctx, task):
    try:
        data = json.loads(task.payload())
    except json.JSONDecodeError:
        raise SkipRetry("无效 JSON")  # 直接归档，不重试

    try:
        await call_api(data)
    except TimeoutError:
        raise  # 普通异常 → 自动重试（默认 25 次，指数退避）
```

默认重试延迟：`seconds = n^4 + 15 + rand(0..30) × (n+1)`

## 14. 监控 & 运维

```bash
# 消费者在线状态
redis-cli ZRANGE asynq:servers 0 -1

# 队列统计
redis-cli LLEN "asynq:{default}:pending"
redis-cli ZCARD "asynq:{default}:scheduled"

# Web 面板（Go 工具，兼容 asynq Key 结构）
go install github.com/hibiken/asynqmon@latest
asynqmon --redis-addr=127.0.0.1:6379
```

也可以通过 Server list API 查询：

```python
from asyrq.internal.rdb import RDB
broker = RDB(redis_client)
servers = await broker.list_servers()
stats = await broker.current_queue_stats("default")
```

## 15. 爬虫场景最佳实践

### 典型爬虫任务流

```
爬虫调度器  ──→  asyrq 入队  ──→  Redis  ──→  消费者抓取
    │                                            │
    │              ← 结果写入 Redis ←────────────┘
    ▼
  URL 状态/数据存储
```

### 消费者示例

```python
app = Server(
    RedisClientOpt(addr="127.0.0.1:6379", password=""),
    Config(name="爬虫消费者", code="crawler-01", concurrency=5),
)

_crawl_sem = asyncio.Semaphore(3)  # 同时最多 3 个请求

@app.task("crawl:page")
async def crawl_page(ctx: Context, task: Task):
    async with _crawl_sem:
        data = json.loads(task.payload())
        url = data["url"]

        # 抓取
        html = await fetch(url)

        # 结果自动写到 crawl:page:result:{task_id}
        writer = task.result_writer()
        await writer.write(json.dumps({"url": url, "html": html}).encode())

asyncio.run(app.run())
```

### 生产者示例

```python
client = Client(RedisClientOpt(addr="127.0.0.1:6379"))

# 批量入队 URL
for url in urls:
    await client.enqueue(
        Task("crawl:page", json.dumps({"url": url}).encode()),
        MaxRetry(3),           # 最多重试 3 次
        Unique(600),           # 10 分钟内去重
    )

# 结果查询
# redis-cli GET crawl:page:result:{task_id}
```

### 为什么用 List 不用 Streams

| 爬虫需求 | List 方案 |
|---------|----------|
| 入队 URL | ✅ Lua 原子入队 |
| 多 worker 并发 | ✅ Semaphore 控制 |
| 重试失败 | ✅ 指数退避 |
| 去重 | ✅ Unique(ttl) |
| 延迟抓取 | ✅ ProcessIn / Cron |
| 监控 | ✅ asynqmon 面板 |
| 消息回溯 | 不需要 |

爬虫任务不需要 Streams 的消息回溯和消费者组功能，List 方案更简洁稳定。
