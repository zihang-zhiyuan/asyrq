# asyrq 使用说明文档

> Python 1:1 复刻 Go 语言 [asynq](https://github.com/hibiken/asynq) 的 Redis 分布式任务队列

---

## 目录

1. [安装与环境要求](#1-安装与环境要求)
2. [快速入门](#2-快速入门)
3. [核心概念](#3-核心概念)
4. [Client — 任务生产者](#4-client--任务生产者)
5. [Server — 任务消费者](#5-server--任务消费者)
6. [ServeMux — 任务路由器](#6-servemux--任务路由器)
7. [Middleware — 中间件](#7-middleware--中间件)
8. [Scheduler — 定时调度器](#8-scheduler--定时调度器)
9. [Options — 任务选项详解](#9-options--任务选项详解)
10. [任务生命周期](#10-任务生命周期)
11. [Redis 连接配置](#11-redis-连接配置)
12. [错误处理](#12-错误处理)
13. [日志配置](#13-日志配置)
14. [高级特性](#14-高级特性)
15. [与 Go asynq 的 API 对照表](#15-与-go-asynq-的-api-对照表)
16. [常见问题](#16-常见问题)

---

## 1. 安装与环境要求

### 环境要求

| 组件 | 最低版本 |
|------|---------|
| Python | 3.9+ |
| Redis | 4.0+（需要 Lua 脚本支持） |

### 安装

```bash
pip install asyrq
```

### 开发模式安装

```bash
git clone <repo-url>
cd asyrq
pip install -e ".[dev]"
```

---

## 2. 快速入门

### 2.1 启动 Redis

```bash
redis-server
```

### 2.2 定义任务处理器

```python
# handlers.py
import json
from asyrq import Handler, Context, Task

# 方式一：函数式处理器
async def handle_email_send(ctx: Context, task: Task):
    """发送邮件的任务处理器"""
    data = json.loads(task.payload().decode("utf-8"))
    to_addr = data["to"]
    subject = data["subject"]
    body = data["body"]

    print(f"发送邮件: to={to_addr}, subject={subject}")
    # 实际的邮件发送逻辑...
    # await send_email(to_addr, subject, body)

    # 返回 None 表示成功，任务进入 Completed 状态
    return None


# 方式二：类式处理器
class ImageProcessor(Handler):
    """图片处理的任务处理器"""

    async def process_task(self, ctx: Context, task: Task):
        data = json.loads(task.payload().decode("utf-8"))
        image_url = data["url"]

        print(f"处理图片: {image_url}")
        # await resize_image(image_url)
        return None
```

### 2.3 入队任务（生产者端）

```python
# producer.py
import asyncio
import json
from asyrq import Task, Client, RedisClientOpt, Queue, MaxRetry

async def main():
    # 创建客户端，连接到本地 Redis
    client = Client(RedisClientOpt(addr="127.0.0.1:6379"))

    # 创建一个邮件发送任务
    payload = json.dumps({
        "to": "user@example.com",
        "subject": "欢迎注册",
        "body": "感谢您的注册！"
    }).encode("utf-8")

    task = Task("email:send", payload)

    # 入队任务（即时执行）
    info = await client.enqueue(task)
    print(f"任务已入队: id={info.id}, 队列={info.queue}")

    # 入队任务到指定队列，限制重试次数
    info = await client.enqueue(
        task,
        Queue("critical"),   # 高优先级队列
        MaxRetry(3),          # 最多重试 3 次
    )
    print(f"任务已入队: id={info.id}, 队列={info.queue}")

    await client.close()

asyncio.run(main())
```

### 2.4 启动任务处理器（消费者端）

```python
# consumer.py
import asyncio
from asyrq import Server, ServeMux, Config, RedisClientOpt
from handlers import handle_email_send, ImageProcessor

async def main():
    # 创建任务路由器
    mux = ServeMux()
    mux.handle_func("email:send", handle_email_send)
    mux.handle("image:resize", ImageProcessor())

    # 服务器配置
    config = Config(
        concurrency=10,  # 最大并发处理 10 个任务
        queues={         # 队列优先级权重
            "critical": 6,  # 高优先级，权重 6
            "default": 3,   # 默认队列，权重 3
            "low": 1,       # 低优先级，权重 1
        },
    )

    # 创建并启动服务器
    server = Server(RedisClientOpt(addr="127.0.0.1:6379"), config)

    # run() 会阻塞直到收到 SIGINT/SIGTERM 信号
    print("服务器启动中，按 Ctrl+C 停止...")
    await server.run(mux)
    print("服务器已关闭")

asyncio.run(main())
```

---

## 3. 核心概念

### 3.1 架构图

```
┌──────────┐     入队任务     ┌──────────┐     出队任务     ┌──────────┐
│  Client  │ ──────────────▶ │  Redis   │ ──────────────▶ │  Server  │
│ (生产者) │                  │ (消息代理) │                  │ (消费者) │
└──────────┘                  └──────────┘                  └──────────┘
                                                              │
                                                         ┌────▼────┐
                                                         │  Worker  │
                                                         │  × N     │
                                                         └─────────┘

┌──────────┐
│ Scheduler │ ─── 定时触发入队
│ (调度器)  │
└──────────┘
```

### 3.2 关键组件

| 组件 | 职责 | 对应 Go asynq |
|------|------|--------------|
| **Task** | 任务单元：类型名 + 负载数据 | `asynq.Task` |
| **Client** | 任务入队：即时/延迟/去重 | `asynq.Client` |
| **Server** | 任务消费：出队 + 分发 Worker | `asynq.Server` |
| **ServeMux** | 任务路由：类型名 → Handler | `asynq.ServeMux` |
| **Handler** | 任务处理器：业务逻辑 | `asynq.Handler` |
| **Scheduler** | 定时调度：Cron 表达式 | `asynq.Scheduler` |
| **Middleware** | 中间件：横切关注点 | `asynq.MiddlewareFunc` |

### 3.3 任务状态机

```
                    ┌──────────┐
                    │ Enqueued │ ← Client.enqueue()
                    └────┬─────┘
                         ▼
                 ┌───────────────┐
            ┌───▶│   Pending     │◀───────────┐
            │    └───────┬───────┘            │
            │            ▼                     │
            │    ┌───────────────┐     ┌──────┴──────┐
            │    │    Active     │────▶│   Retry     │
            │    └───────┬───────┘     └──────┬──────┘
            │            │                    │ (重试耗尽)
            │            ▼                    ▼
            │    ┌───────────────┐     ┌──────────────┐
            │    │  Completed    │     │   Archived   │
            │    └───────────────┘     └──────────────┘
            │
     ┌──────┴──────┐     ┌──────────────┐
     │  Scheduled  │────▶│   Pending    │
     └─────────────┘     └──────────────┘
```

---

## 4. Client — 任务生产者

### 4.1 创建客户端

```python
from asyrq import Client, RedisClientOpt

# 方式一：从配置创建
client = Client(RedisClientOpt(
    addr="127.0.0.1:6379",
    db=0,
    password="",
    pool_size=10,
))

# 方式二：使用 URI 字符串
from asyrq import parse_redis_uri
client = Client(parse_redis_uri("redis://:password@localhost:6379/0"))

# 方式三：使用已有 Redis 客户端
import redis.asyncio as redis
r = redis.Redis(host="localhost", port=6379)
client = Client(r)
```

### 4.2 创建任务

```python
from asyrq import Task

# 基本任务
task = Task("email:send", b'{"to": "user@test.com"}')

# 带请求头的任务
task = Task("report:generate", payload, headers={"x-trace-id": "abc123"})

# 使用 new_task 工厂函数
from asyrq import new_task
task = new_task("sms:send", payload)
```

### 4.3 入队方式

```python
from asyrq import Queue, MaxRetry, Timeout, Unique, ProcessIn, ProcessAt

# 1. 即时入队 — 任务立即进入队列等待处理
info = await client.enqueue(task)

# 2. 即时入队 + 选项
info = await client.enqueue(
    task,
    Queue("critical"),      # 目标队列
    MaxRetry(5),             # 最大重试 5 次
    Timeout(60),             # 超时 60 秒
)

# 3. 延迟入队 — n 秒后执行
info = await client.enqueue(task, ProcessIn(30))  # 30 秒后执行

# 4. 定时入队 — 指定绝对时间执行
import time
target_time = int(time.time() * 1_000_000_000) + 3600_000_000_000  # 1 小时后
info = await client.enqueue(task, ProcessAt(target_time))

# 5. 唯一入队 — TTL 内去重
info = await client.enqueue(task, Unique(3600))  # 1 小时内不重复

# 6. 定时 + 去重
info = await client.enqueue(task, ProcessIn(60), Unique(300))
```

### 4.4 使用 async with

```python
async with Client(RedisClientOpt(addr="localhost:6379")) as client:
    info = await client.enqueue(task)
    # 离开上下文时自动关闭连接
```

### 4.5 连接检查

```python
ok = await client.ping()  # 返回 True/False
```

---

## 5. Server — 任务消费者

### 5.1 配置项

```python
from asyrq import Server, Config

config = Config(
    # ---- 并发控制 ----
    concurrency=10,           # 最大并发处理任务数（默认 CPU 核数）

    # ---- 队列配置 ----
    queues={                  # 队列名 → 优先级权重
        "critical": 6,        # 高优先级
        "default": 3,         # 中优先级
        "low": 1,             # 低优先级
    },
    strict_priority=False,    # True: 高优先级队列完全清空后才处理低优先级

    # ---- 超时控制 ----
    shutdown_timeout=8,       # 优雅关闭最大等待秒数

    # ---- 健康检查 ----
    health_check_interval=15,  # Redis 健康检查间隔（秒）

    # ---- 任务检查 ----
    delayed_task_check_interval=5,   # 定时/重试任务检查间隔
    task_check_interval=1,           # 空队列时的轮询间隔

    # ---- 任务组聚合 ----
    group_max_delay=30,       # 聚合组最大延迟（秒）
    group_grace_period=60,    # 聚合组宽限期（秒）
    group_max_size=20,        # 聚合组最大任务数
    group_aggregator=None,    # 自定义聚合函数

    # ---- 清理 ----
    janitor_interval=8,       # 已完成任务清理间隔（秒）
    janitor_batch_size=100,   # 每次清理的最大数量
)
```

### 5.2 启动方式

```python
# 方式一：阻塞运行（推荐用于独立进程）
await server.run(mux)  # 阻塞直到收到信号，自动优雅关闭

# 方式二：非阻塞启动
await server.start(mux)  # 后台启动
# ... 做其他事情 ...
await server.shutdown()   # 手动关闭

# 方式三：逐步控制
await server.start(mux)
await server.quiet()     # 停止接受新任务，等待当前任务完成
await server.stop()      # 立即停止
await server.shutdown()  # 完整关闭（清理 Redis 状态）
```

### 5.3 后台协程

Server 启动后会在后台运行以下协程：

| 协程 | 职责 | 默认间隔 |
|------|------|---------|
| **Processor** | 核心循环：持续出队 → 分发给 Worker | 实时 |
| **Heartbeat** | 定期写入服务器状态 + 延长任务租约 | 5 秒 |
| **Forwarder** | 将到期任务从 scheduled/retry 前移到 pending | 5 秒 |
| **Recoverer** | 检测并恢复孤儿任务（Worker 崩溃后遗留） | 60 秒 |
| **Janitor** | 删除已过保留期的完成任务 | 8 秒 |
| **Aggregator** | 检查并聚合任务组 | 2 秒 |
| **Healthcheck** | 检查 Redis 连接状况 | 15 秒 |

### 5.4 自定义重试策略

```python
from asyrq import Config

def my_retry_delay(retry_count: int, error: Exception, task: Task) -> int:
    """自定义重试延迟函数 — 固定间隔重试"""
    return 10  # 每次重试都等 10 秒


def my_is_failure(error: Exception) -> bool:
    """自定义失败判定 — 只有特定错误才算失败"""
    if isinstance(error, ValueError):
        return False  # ValueError 不算失败，不计入统计
    return True


config = Config(
    retry_delay_func=my_retry_delay,
    is_failure_func=my_is_failure,
)
```

### 5.5 错误回调

```python
async def my_error_handler(ctx, task, error):
    """任务处理失败时的回调 — 可用于告警通知"""
    print(f"[ALERT] 任务失败: type={task.type()}, error={error}")
    # await send_alert(f"任务 {task.type()} 失败: {error}")

config = Config(error_handler=my_error_handler)
```

---

## 6. ServeMux — 任务路由器

### 6.1 基本用法

```python
from asyrq import ServeMux

mux = ServeMux()

# 精确匹配
mux.handle_func("email:send", handle_email_send)
mux.handle_func("email:notify", handle_email_notify)
mux.handle_func("report:daily", handle_daily_report)

# 前缀匹配（以 ":" 结尾的模式）
mux.handle_func("image:", handle_image_default)    # 匹配所有 "image:*"
mux.handle_func("notification:", handle_default)   # 匹配所有 "notification:*"
```

### 6.2 路由匹配规则

匹配优先级：**精确匹配 > 最长前缀匹配**

```
已注册模式：
  "email:"  → default_email_handler
  "email:send" → send_handler

请求匹配：
  "email:send"    → send_handler        (精确匹配)
  "email:receive" → default_email_handler (前缀匹配)
  "sms:send"      → 抛出 ValueError      (无匹配)
```

### 6.3 嵌套路由器

```python
# 创建子路由器
email_mux = ServeMux()
email_mux.use(rate_limit_middleware)  # 子路由器专属中间件
email_mux.handle_func("email:send", send_handler)
email_mux.handle_func("email:notify", notify_handler)

# 主路由器
main_mux = ServeMux()
main_mux.use(logging_middleware)
main_mux.handle("email:", email_mux)  # 嵌套

# Server 可以直接用 ServeMux（它实现了 Handler 接口）
await server.run(main_mux)
```

---

## 7. Middleware — 中间件

### 7.1 中间件模型（洋葱模型）

```
请求进入
   │
   ▼
┌─────────┐   ┌─────────┐   ┌─────────┐   ┌──────────┐
│ 中间件A  │──▶│ 中间件B  │──▶│ 中间件C  │──▶│ Handler  │
│ (before) │   │ (before) │   │ (before) │   │ (核心)   │
│ (after)  │◀──│ (after)  │◀──│ (after)  │◀──│         │
└─────────┘   └─────────┘   └─────────┘   └──────────┘
   │
   ▼
响应返回
```

### 7.2 编写中间件

```python
from asyrq import Handler, MiddlewareFunc, Context, Task
import time

# 示例 1：计时中间件
def timing_middleware() -> MiddlewareFunc:
    """统计每个任务的处理耗时"""
    def middleware(handler: Handler) -> Handler:
        class TimingHandler(Handler):
            async def process_task(self, ctx: Context, task: Task):
                start = time.time()
                try:
                    await handler.process_task(ctx, task)
                finally:
                    elapsed = time.time() - start
                    print(f"任务 {task.type()} 耗时 {elapsed:.2f}s")
        return TimingHandler()
    return middleware


# 示例 2：重试计数中间件
def retry_counter_middleware() -> MiddlewareFunc:
    """为任务添加重试计数信息"""
    def middleware(handler: Handler) -> Handler:
        class RetryCounterHandler(Handler):
            async def process_task(self, ctx: Context, task: Task):
                # 在处理前记录
                print(f"处理任务: {task.type()}")
                await handler.process_task(ctx, task)
        return RetryCounterHandler()
    return middleware
```

### 7.3 注册中间件

```python
mux = ServeMux()

# 注册中间件（按注册顺序执行）
mux.use(
    timing_middleware(),
    retry_counter_middleware(),
    logging_middleware(),
)

# 注册业务处理器
mux.handle_func("email:send", handle_email_send)
```

---

## 8. Scheduler — 定时调度器

### 8.1 基本用法

```python
import asyncio
from asyrq import Scheduler, Task, RedisClientOpt, Queue

async def main():
    scheduler = Scheduler(RedisClientOpt(addr="127.0.0.1:6379"))

    # 注册周期性任务
    daily_report = Task("report:daily", b'{"type": "sales"}')

    # 使用 Cron 表达式 — 每天早上 9:00
    await scheduler.register("0 9 * * *", daily_report)

    # 使用 @every 格式 — 每 5 分钟
    await scheduler.register("@every 5m", Task("health:check"))

    # 使用 @every 格式 — 每 30 秒
    await scheduler.register("@every 30s", Task("cache:warmup"))

    # 带选项的注册
    await scheduler.register(
        "0 */2 * * *",
        daily_report,
        Queue("critical"),
        MaxRetry(3),
    )

    # 启动调度器（阻塞运行）
    await scheduler.run()

asyncio.run(main())
```

### 8.2 Cron 表达式支持

| 格式 | 示例 | 说明 |
|------|------|------|
| 5 字段 Cron | `"0 9 * * *"` | 每天 9:00 |
| 5 字段 Cron | `"*/5 * * * *"` | 每 5 分钟 |
| 5 字段 Cron | `"30 8 * * 1-5"` | 工作日 8:30 |
| 6 字段 Cron | `"0 */5 * * * *"` | 每 5 分钟（带秒字段） |
| @every | `"@every 1m"` | 每分钟 |
| @every | `"@every 30s"` | 每 30 秒 |
| @every | `"@every 2h30m"` | 每 2 小时 30 分钟 |
| @every | `"@every 1d"` | 每天 |

### 8.3 取消注册

```python
# 注册时获取条目 ID
entry_id = await scheduler.register("@every 1h", task)

# 后续取消注册
await scheduler.unregister(entry_id)
```

### 8.4 调度器回调

```python
from asyrq import SchedulerOpts

async def before_enqueue(task: Task):
    """任务入队前的回调"""
    print(f"即将入队: {task.type()}")

async def after_enqueue(task_info, error):
    """任务入队后的回调"""
    if error:
        print(f"入队失败: {error}")
    else:
        print(f"入队成功: {task_info.id}")

async def on_enqueue_error(task, opts, error):
    """入队错误处理 — 发送告警"""
    await send_alert(f"调度入队失败: {task.type()}, {error}")

opts = SchedulerOpts(
    pre_enqueue_func=before_enqueue,
    post_enqueue_func=after_enqueue,
    enqueue_error_handler=on_enqueue_error,
)

scheduler = Scheduler(RedisClientOpt(addr="localhost:6379"), opts)
```

---

## 9. Options — 任务选项详解

### 9.1 全部选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `MaxRetry(n)` | `int` | 25 | 最大重试次数 |
| `Queue(name)` | `str` | `"default"` | 目标队列名 |
| `TaskID(id)` | `str` | 自动 UUID | 自定义任务 ID |
| `Timeout(secs)` | `int` | 0（无限制） | 处理超时秒数 |
| `Deadline(nsec)` | `int` | 0（无限制） | 截止时间戳（纳秒） |
| `Unique(ttl)` | `int` | 不启用 | 唯一锁 TTL（秒） |
| `ProcessIn(secs)` | `int` | 不启用 | 延迟秒数后执行 |
| `ProcessAt(nsec)` | `int` | 不启用 | 指定时间戳执行 |
| `Retention(secs)` | `int` | 0（不保留） | 完成后保留秒数 |
| `Group(name)` | `str` | 不启用 | 任务聚合组名 |

### 9.2 选项组合示例

```python
# 高优任务：关键队列 + 少重试
await client.enqueue(task, Queue("critical"), MaxRetry(1))

# 幂等任务：去重 + 延迟
await client.enqueue(task, Unique(600), ProcessIn(30))

# 长任务：长超时 + 结果保留
await client.enqueue(task, Timeout(300), Retention(3600))

# 批量任务：聚合组
await client.enqueue(task, Group("batch-hourly"))
```

### 9.3 选项冲突

以下选项互斥，同时使用会抛出 `ValueError`：

```python
# ❌ 错误：不能同时设置 ProcessAt 和 ProcessIn
await client.enqueue(task, ProcessAt(t1), ProcessIn(30))

# ✅ 正确：只使用一个
await client.enqueue(task, ProcessIn(30))
```

---

## 10. 任务生命周期

### 10.1 任务从入队到完成的流程

```
1. Client.enqueue(task)
   │
   ▼
2. Redis: Pending 队列
   │
   ▼
3. Server: Dequeue（原子 Lua 脚本）
   │
   ▼
4. Redis: Active 列表（设置租约）
   │
   ▼
5. Worker: Handler.process_task()
   │
   ├── 成功 → Done → Completed（可选保留）
   └── 失败 → Retry → Pending → Active → ...
               └── 重试耗尽 → Archived
```

### 10.2 重试策略

默认使用指数退避重试：

```python
# 默认重试延迟公式：
# seconds = n^4 + 15 + random(0, 30) * (n + 1)

# 示例延迟：
# 第 0 次重试: ~15-45 秒
# 第 1 次重试: ~16-76 秒  (1^4 + 15 + rand*2)
# 第 2 次重试: ~31-121 秒 (16 + 15 + rand*3)
# 第 3 次重试: ~96-216 秒 (81 + 15 + rand*4)
# 第 5 次重试: ~640-820 秒
```

### 10.3 孤儿任务恢复

当 Worker 崩溃时，Active 状态的任务租约会过期（默认 30 秒）。
Recoverer 协程（每 60 秒检查一次）会检测过期租约并重新入队：

```
Worker 崩溃
    │
    ▼ (30 秒后租约过期)
Recoverer 检测到孤儿任务
    │
    ├── 重试次数未耗尽 → 重新入队 Pending
    └── 重试次数已耗尽 → 归档到 Archived
```

---

## 11. Redis 连接配置

### 11.1 独立 Redis

```python
from asyrq import RedisClientOpt

opt = RedisClientOpt(
    addr="redis.example.com:6379",
    password="your_password",     # Redis 认证密码
    username="your_username",     # Redis 6+ ACL 用户名
    db=0,                         # 数据库编号
    dial_timeout=5,               # 连接超时（秒）
    read_timeout=3,               # 读超时（秒）
    write_timeout=3,              # 写超时（秒）
    pool_size=10,                 # 连接池大小
    tls_config=ssl_context,       # TLS 配置（可选）
)
```

### 11.2 URI 解析

```python
from asyrq import parse_redis_uri

# 基本连接
opt = parse_redis_uri("redis://localhost:6379")

# 带密码
opt = parse_redis_uri("redis://:mypassword@localhost:6379")

# 指定数据库
opt = parse_redis_uri("redis://localhost:6379/5")

# TLS 连接
opt = parse_redis_uri("rediss://localhost:6380")

# 带完整参数
opt = parse_redis_uri("redis://:pwd@host:6379/3?dial_timeout=10&pool_size=20")
```

### 11.3 Redis Cluster（规划中）

```python
from asyrq import RedisClusterClientOpt

opt = RedisClusterClientOpt(
    addrs=["node1:6379", "node2:6379", "node3:6379"],
    max_redirects=8,
)
```

### 11.4 Redis Sentinel（规划中）

```python
from asyrq import RedisFailoverClientOpt

opt = RedisFailoverClientOpt(
    master_name="mymaster",
    sentinel_addrs=["sentinel1:26379", "sentinel2:26379"],
)
```

---

## 12. 错误处理

### 12.1 错误类型

| 错误 | 用途 | 触发时机 |
|------|------|---------|
| `SkipRetry` | 跳过重试，直接归档 | Handler 返回此错误 |
| `EnqueueError` | 入队失败 | Client 入队异常 |
| `DequeueError` | 出队失败 | Server 出队异常 |
| `TaskNotFoundError` | 任务未找到 | 查询任务不存在 |
| `QueueNotFoundError` | 队列未找到 | 操作不存在的队列 |

### 12.2 SkipRetry 使用

```python
from asyrq import SkipRetry

async def handle_task(ctx, task):
    data = json.loads(task.payload())

    # 无效的任务数据 — 不应重试
    if not data.get("user_id"):
        raise SkipRetry("缺少 user_id 参数")

    try:
        await process(data)
    except ValueError as e:
        # 数据格式错误 — 不应重试
        raise SkipRetry(f"数据格式错误: {e}") from e
    except TimeoutError:
        # 网络超时 — 应该重试
        raise  # 普通异常会触发重试
```

### 12.3 判断是否为 SkipRetry

```python
from asyrq import SkipRetry

try:
    await my_handler(ctx, task)
except SkipRetry:
    print("任务被跳过，不会重试")
except Exception as e:
    print(f"任务失败，将重试: {e}")
```

---

## 13. 日志配置

### 13.1 使用默认日志器

```python
from asyrq import Server, Config, LogLevel

config = Config(
    log_level=LogLevel.DEBUG,  # 输出详细日志
)
```

### 13.2 自定义日志器

```python
from asyrq import Logger, LogLevel
import logging

# 方式一：包装 Python logging
class CustomLogger(Logger):
    def __init__(self):
        self._logger = logging.getLogger("my_app.asynq")
        self._logger.setLevel(logging.DEBUG)

    def debug(self, *args, **kwargs):
        self._logger.debug(*args, **kwargs)

    def info(self, *args, **kwargs):
        self._logger.info(*args, **kwargs)

    def warn(self, *args, **kwargs):
        self._logger.warning(*args, **kwargs)

    def error(self, *args, **kwargs):
        self._logger.error(*args, **kwargs)

    def fatal(self, *args, **kwargs):
        self._logger.critical(*args, **kwargs)


config = Config(logger=CustomLogger())
```

### 13.3 日志级别

| 级别 | 值 | 用途 |
|------|---|------|
| `LogLevel.DEBUG` | 10 | 开发调试 |
| `LogLevel.INFO` | 20 | 一般信息（默认） |
| `LogLevel.WARN` | 30 | 警告 |
| `LogLevel.ERROR` | 40 | 错误 |
| `LogLevel.FATAL` | 50 | 致命错误 |

---

## 14. 高级特性

### 14.1 任务结果查询

```python
# 入队时指定保留时间
info = await client.enqueue(task, Retention(3600))  # 完成后保留 1 小时

# 在 Handler 中写入结果
async def my_handler(ctx, task):
    result = await do_work(task.payload())

    # 通过 ResultWriter 写入结果
    writer = task.result_writer()
    if writer:
        await writer.write(json.dumps(result).encode())

    return None
```

### 14.2 队列管理

```python
# 暂停/恢复队列
from asyrq.internal.rdb import RDB
import redis.asyncio as redis

r = redis.Redis(host="localhost", port=6379)
broker = RDB(r)

await broker.pause_queue("low")     # 暂停处理
await broker.unpause_queue("low")   # 恢复处理
```

### 14.3 队列状态查询

```python
# 获取队列统计
stats = await broker.current_queue_stats("default")
print(stats)
# {
#   "pending": 10,     # 等待中
#   "active": 2,       # 处理中
#   "scheduled": 3,    # 定时等待
#   "retry": 1,        # 等待重试
#   "archived": 0,     # 已归档
#   "completed": 5,    # 已完成
# }

# 列出所有队列
queues = await broker.list_queues()
print(queues)  # ["critical", "default", "low"]

# 列出在线服务器
servers = await broker.list_servers()
```

### 14.4 任务聚合（Group Aggregation）

```python
from asyrq import Group

# 将任务放入聚合组
await client.enqueue(task, Group("batch-hourly"))
await client.enqueue(task2, Group("batch-hourly"))
await client.enqueue(task3, Group("batch-hourly"))

# Server 配置聚合
def my_aggregator(group_name: str, tasks: list) -> Task:
    """将一组任务合并为一个批处理任务"""
    all_data = [json.loads(t.payload()) for t in tasks]
    return Task("batch:process", json.dumps(all_data).encode())

config = Config(
    group_max_delay=30,          # 30 秒后强制聚合
    group_grace_period=60,       # 60 秒宽限期
    group_max_size=20,           # 20 个任务后强制聚合
    group_aggregator=my_aggregator,
)
```

### 14.5 部署建议

```python
# 生产环境推荐配置
config = Config(
    concurrency=min(32, os.cpu_count() * 4),  # 根据 CPU 核数调整
    queues={
        "critical": 6,
        "default": 3,
        "low": 1,
    },
    shutdown_timeout=30,        # 生产环境给更长的关闭等待
    retry_delay_func=my_retry_delay,   # 自定义重试策略
    error_handler=my_error_handler,    # 配置告警
)
```

---

## 15. 与 Go asynq 的 API 对照表

| Go asynq | asyrq |
|----------|----------|
| `asynq.NewTask("type", payload)` | `Task("type", payload)` |
| `asynq.NewTask("type", payload, opts...)` | `Task("type", payload, *opts)` |
| `client.Enqueue(task)` | `await client.enqueue(task)` |
| `client.Enqueue(task, opts...)` | `await client.enqueue(task, *opts)` |
| `asynq.Queue("name")` | `Queue("name")` |
| `asynq.MaxRetry(n)` | `MaxRetry(n)` |
| `asynq.Timeout(d)` | `Timeout(seconds)` |
| `asynq.Deadline(t)` | `Deadline(nano_timestamp)` |
| `asynq.Unique(ttl)` | `Unique(seconds)` |
| `asynq.ProcessIn(d)` | `ProcessIn(seconds)` |
| `asynq.ProcessAt(t)` | `ProcessAt(nano_timestamp)` |
| `asynq.Retention(d)` | `Retention(seconds)` |
| `asynq.Group(name)` | `Group(name)` |
| `asynq.TaskID(id)` | `TaskID(id)` |
| `task.Type()` | `task.type()` |
| `task.Payload()` | `task.payload()` |
| `task.ResultWriter()` | `task.result_writer()` |
| `mux.Handle(p, h)` | `mux.handle(p, h)` |
| `mux.HandleFunc(p, f)` | `mux.handle_func(p, f)` |
| `mux.Use(mw...)` | `mux.use(*mws)` |
| `server.Run(mux)` | `await server.run(mux)` |
| `server.Start(mux)` | `await server.start(mux)` |
| `server.Shutdown()` | `await server.shutdown()` |
| `server.Stop()` | `await server.stop()` |
| `server.Quiet()` | `await server.quiet()` |
| `scheduler.Register(spec, task)` | `await scheduler.register(spec, task)` |
| `scheduler.Register(spec, task, opts...)` | `await scheduler.register(spec, task, *opts)` |
| `scheduler.Unregister(id)` | `await scheduler.unregister(id)` |
| `scheduler.Run()` | `await scheduler.run()` |
| `scheduler.Shutdown()` | `await scheduler.shutdown()` |
| `RedisClientOpt{Addr: "..."}` | `RedisClientOpt(addr="...")` |
| `ParseRedisURI(uri)` | `parse_redis_uri(uri)` |
| `asynq.SkipRetry` | `SkipRetry` |
| `context.Context` | `Context` |

---

## 16. 常见问题

### Q1: 如何确保任务不丢失？

asyrq 保证 **至少一次执行**（at-least-once）：
- 任务先写入 Redis 持久化存储，然后才从队列中移除
- 如果 Worker 崩溃，租约过期后 Recoverer 会将任务重新入队
- 建议在 Handler 中实现幂等逻辑，以应对可能的重复执行

### Q2: 如何实现任务优先级？

通过队列权重配置：

```python
config = Config(
    queues={
        "critical": 6,  # 约 60% 的处理能力
        "default": 3,   # 约 30% 的处理能力
        "low": 1,       # 约 10% 的处理能力
    },
)
# 或使用严格优先级
config = Config(queues={...}, strict_priority=True)
# 严格优先级下必须先清空高优先级队列才处理低优先级
```

### Q3: 如何处理超长任务？

```python
# 设置较长超时
await client.enqueue(task, Timeout(600))  # 10 分钟超时

# 或在 Handler 中定期检查取消信号
async def my_handler(ctx, task):
    for chunk in large_data:
        if ctx.cancelled:
            return  # 任务被取消，停止处理
        await process_chunk(chunk)
```

### Q4: 如何监控任务队列？

```python
# 使用内置的 Broker 查询队列状态
stats = await broker.current_queue_stats("default")
print(f"待处理: {stats['pending']}, 处理中: {stats['active']}")

# 兼容 asynqmon Web 监控面板
# asyrq 的 Redis Key 结构与 Go asynq 完全一致
# 可以直接使用 asynqmon 进行监控
```

### Q5: Windows 兼容性如何？

- 信号处理：Windows 不支持 `add_signal_handler`，用 `asyncio.Event` 代替
- 其余所有功能在 Windows/Linux/macOS 上均可正常使用

### Q6: 生产环境推荐配置？

```python
# 生产环境推荐
config = Config(
    concurrency=32,
    queues={"default": 1},
    shutdown_timeout=30,
    health_check_interval=15,
    retry_delay_func=custom_retry_delay,
    error_handler=alert_error_handler,
)
```
